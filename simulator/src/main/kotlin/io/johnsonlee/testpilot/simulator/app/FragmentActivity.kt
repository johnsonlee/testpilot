package io.johnsonlee.testpilot.simulator.app

import io.johnsonlee.testpilot.simulator.activity.Activity

/**
 * Activity subclass that serves as a bytecode mapping target for
 * `androidx.fragment.app.FragmentActivity` and `androidx.appcompat.app.AppCompatActivity`.
 */
open class FragmentActivity : Activity()
